{/* Modal.mdx */}
 
import { Canvas, Title, Subtitle, ArgTypes} from '@storybook/blocks';
 
import * as ModalStories from '../core/modal.stories';
import ForwardRefNote from '../shared/ForwardRefNote';

<Title  /> 

<Subtitle />

## Basic Modal

You should use specific components for modal content: `ModalTitle`, `ModalContent`, and `ModalActions`.

You can use check each component in the storybook to see how they work.

<Canvas of={ModalStories.Primary} withToolbar layout='centered'/>

## API

<ArgTypes sort='alpha' />

<ForwardRefNote />