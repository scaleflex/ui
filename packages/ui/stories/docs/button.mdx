{/* Button.mdx */}
 
import { Canvas, Meta, Title, Subtitle, ArgTypes} from '@storybook/blocks';
 
import * as ButtonStories from '../core/button.stories';
import ForwardRefNote from '../shared/ForwardRefNote';
import { ButtonColor } from '../../src/utils/types/color/button-color';


<Title  /> 

<Subtitle />

## Colors

Button colors are:

{Object.values(ButtonColor).map((color, index) => `${color}${Object.values(ButtonColor).length - 1 !== index ? ', ' : ''}`)}.

<Canvas of={ButtonStories.Primary} withToolbar layout='centered'/>

## Sizes

For larger or smaller buttons, use the `size` prop.

<Canvas of={ButtonStories.Sizes} withToolbar layout='centered' />

## Buttons with icons and badge

<Canvas of={ButtonStories.WithIconsAndBadge} withToolbar layout='centered' />

## Loading, active and disabled buttons

<Canvas of={ButtonStories.OtherStates} withToolbar layout='centered' />

## Other button types

To change button type, use the `buttonType` prop. With button type `sidebar` you can use `sidebarType` prop to change the arrows and labels direction.

<Canvas of={ButtonStories.SideBar} withToolbar layout='centered' />

## API

<ArgTypes sort='alpha' />

<ForwardRefNote />
